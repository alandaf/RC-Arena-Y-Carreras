# Variables para docker-compose.prod.yml — copia este archivo a ".env" en el VPS
# (docker compose lee ".env" automáticamente) y completa con valores reales.

DOMAIN=tudominio.cl
VITE_API_URL=https://tudominio.cl

POSTGRES_DB=rcarena
POSTGRES_USER=rcarena_user
POSTGRES_PASSWORD=cambia-esta-clave-por-una-segura
